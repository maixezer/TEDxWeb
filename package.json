{
  "dependencies": {
    "bootstrap": "4.0.0-beta",
    "express": "^4.15.4",
    "jquery": "^3.2.1",
    "module-alias": "^2.0.1",
    "moment-timezone": "^0.5.13",
    "next": "^3.0.6",
    "normalize.css": "^7.0.0",
    "pm2": "^2.6.1",
    "preact": "^8.2.1",
    "preact-compat": "^3.17.0",
    "react-scrollable-anchor": "^0.5.0"
  },
  "scripts": {
    "dev": "next",
    "build": "cross-env NODE_ENV=production next build",
    "start": "cross-env NODE_ENV=production next start",
    "restart": "pm2 startOrRestart ecosystem.config.js",
    "ec2deploy": "pm2 deploy ecosystem.config.js production",
    "predeploy": "yarn run build && cross-env NODE_ENV=production next export && touch out/.nojekyll && echo \"ppanya.me\" > out/CNAME",
    "deploy": "git add -A && git commit -m \"update website\" && git subtree split --prefix out -b gh-pages && git push origin gh-pages:gh-pages --force && git branch -D gh-pages"
  },
  "devDependencies": {
    "babel-plugin-module-resolver": "^2.7.1",
    "babel-plugin-wrap-in-js": "^1.1.1",
    "babel-preset-env": "^1.6.0",
    "cross-env": "^5.0.5",
    "node-sass": "^4.5.3",
    "postcss": "^6.0.9",
    "postcss-cssnext": "^3.0.2",
    "postcss-easy-import": "^2.1.0",
    "postcss-loader": "^2.0.6",
    "raw-loader": "^0.5.1",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "sass-loader": "^6.0.6"
  }
}
